

validate(file)
    gtfs-to-geojson(file)
    transport-validator(file)



validate-on-the-fly(%Plug.Conn{} = conn, %{"upload" => upload_params})
    renomage du sfichier
    validate(file)


validate-backoffice(url)
    file = download(url)
    validate(file)


get_geojson(file)
    #cree un geojson depuis un fichier

get_geojson(url)
    #cree un geojson depuis une url
    file = download
    get_geojson(file)