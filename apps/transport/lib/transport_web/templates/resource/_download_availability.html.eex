<div class="panel">
  <div class="download_availability">
    <div class="flex_container">
      <%= for day_data <- @uptime_per_day do %>
      <% uptime = day_data["uptime"] * 100 %>
      <div class="day tooltip <%= download_availability_class(uptime) %>">
        <span class="tooltiptext">
          <div><%= date_to_string(@conn, day_data["day"]) %></div>
          <div class="<%= download_availability_class_text(uptime) %>">
            <%= round_float(uptime, 1) %>%
          </div>
        </span>
      </div>
      <% end %>
    </div>
  </div>
  <details class="pt-24">
    <summary><%= dgettext("page-dataset-details", "Learn more") %></summary>
    <%= dgettext("page-dataset-details", "We test this resource download availability every hour. If we detect a downtime, we perform subsequent tests every 10 minutes, until the resource is back online.") %>
  </details>
</div>
